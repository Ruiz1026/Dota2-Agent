---
description: 装备与 item_id 映射规范（禁止猜测）
---
当用户涉及装备解析、item_id、出装明细时，必须遵守：

1. 映射流程：
- 优先调用 `get_item_id_map`
- 仅基于返回的 name/key 解释装备

2. 禁止事项：
- 禁止凭经验猜测 item_id 对应装备
- 禁止把未映射条目写成确定装备名

3. 缺失处理：
- 映射不存在时标注“未知装备”
- 继续基于可确认的数据完成分析

4. 输出结构建议：
- 先列映射结果，再给结论
- 对未知条目单独列出，避免混淆
