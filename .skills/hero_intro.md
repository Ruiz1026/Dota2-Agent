---
description: 英雄介绍优先走本地 RAG（rag_hero_intro）
---
当用户请求英雄介绍、背景、技能概览或玩法简介时：

1. 必须优先调用 `rag_hero_intro` 获取本地知识库内容。
2. 回答必须忠于 RAG 结果，不得补造设定或技能细节。
3. 若用户还要求统计数据（胜率/对线/出装/排行），再追加调用相关 `get_hero*` 工具。
4. 若英雄 ID 未知，可先调用 `get_heroes` 或 `get_constants` 做映射。
5. 若工具失败或无数据，明确写“数据未提供/无法确认”。
